# depmod configuration for ubturbo smap subpackage
# Registers the external smap kernel modules directory so that depmod
# can find the modules under /lib/modules/smap/ on kernel 6.6.*.
external 6.6.* /lib/modules/smap
