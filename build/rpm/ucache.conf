# depmod configuration for ubturbo ucache subpackage
# Registers the external ucache kernel module directory so that depmod
# can find the module under /lib/modules/ucache/ on kernel 6.6.*.
external 6.6.* /lib/modules/ucache
